# --- Local dev (works out of the box) ---
DATABASE_URL="file:./dev.db"

# --- Production integration points (stubbed in the MVP) ---
# Swap SQLite for Postgres (Supabase / Neon) in production:
# DATABASE_URL="postgresql://user:pass@host:5432/spreedesk"

# Auth (MVP uses mock cookie auth; replace with NextAuth in production)
# NEXTAUTH_SECRET=""
# NEXTAUTH_URL="http://localhost:3000"

# Stripe (booking + subscriptions) — see src/app/api/stripe/route.ts
# STRIPE_SECRET_KEY=""
# STRIPE_WEBHOOK_SECRET=""

# Mapbox (Explore map) — Explore currently renders a static map placeholder
# NEXT_PUBLIC_MAPBOX_TOKEN=""

# Pusher (real-time notifications)
# PUSHER_APP_ID=""
# PUSHER_KEY=""
# PUSHER_SECRET=""
